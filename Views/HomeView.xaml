<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="NSSImporter.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:conv="using:NSSImporter.Converters"
    mc:Ignorable="d">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Padding="16" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="NSSImporter Tool" FontSize="24" FontWeight="SemiBold"/>
            <TextBlock Text="For use with Nilesoft Shell" Opacity="0.78"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,16,0,12" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
            <TextBlock Text="Nilesoft Shell path:" VerticalAlignment="Center"/>
            <TextBox x:Name="ShellPathBox" Width="520" />
            <Button x:Name="BrowseBtn" Content="Browse..." />
            <Button x:Name="DetectBtn" Content="Detect" />
            <Button x:Name="StartBtn" Content="Start Import" />
            <Button x:Name="RestartBtn" Content="Restart Shell" />
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="ShellStatus" Grid.Row="0" Text="Status: Idle" />
            <Border Grid.Row="1" Margin="0,6,0,0" Padding="8" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6">
                <ScrollViewer>
                    <TextBlock x:Name="StatusText" TextWrapping="Wrap"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
